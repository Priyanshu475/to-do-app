var call_counter = require('./call_counter');

function add(x,y){
     call_counter();
     return x + y;
}

function substract(x,y){
     call_counter();
     return x - y;
}


module.exports = {
     adition: add,
     substraction: substract
}